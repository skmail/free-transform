{
  "name": "free-transform-lerna",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "bootstrap": "npx lerna bootstrap",
    "test": "echo \"Error: no test specified\" && exit 1",
    "demo:vue": "npm run dexv --prefix ./packages/editor-vue"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.4.3"
  }
}
